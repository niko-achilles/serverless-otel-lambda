#collector.yaml in the root directory
#Set an environemnt variable 'OPENTELEMETRY_COLLECTOR_CONFIG_FILE' to '/var/task/<path/<to>/<file>'

receivers:
  otlp:
    protocols:
      grpc:

exporters:
  logzio:
    account_token: "rZBBfoZFQXnZGleAryDjrKzurBgiyBEU"
    region: "uk"

service:
  pipelines:
    traces:
      receivers: [otlp]
      exporters: [logzio]
